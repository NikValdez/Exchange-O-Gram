<h1>Exchange-O-Gram</h1>

  <% @posts.each do |post| %>
  <div class="row">
    <div class="col-md-12">
      <%= link_to (image_tag post.image.url(:medium)), post_path(post) %>
      <p><%= post.caption %></p>
    </div>
  <% end %>
  </div>
